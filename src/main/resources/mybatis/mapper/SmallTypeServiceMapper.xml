<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.hsms.mapper.SmallTypeServiceMapper">
    <select id="findbyid" parameterType="int" resultType="com.example.hsms.entity.Service_smalltype">
        SELECT * FROM service_smalltype WHERE id = #{id}
    </select>
</mapper>